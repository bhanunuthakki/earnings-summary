# Gemini — earnings-summary

@./AGENTS.md

The repo rulebook is now `AGENTS.md` (tool-agnostic, read by every runtime). Global rules + the Gemini skill-mimic fallback come from the root `C:\Users\Bhanu\.gemini\GEMINI.md` via hierarchical merge.
