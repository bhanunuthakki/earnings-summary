flask
pandas
lxml
# Security floor: CVE-2026-25645 (fixed 2.33.0). CI installs latest; the floor
# stops dev/prod from resolving a known-vulnerable build.
requests>=2.33.0
python-dotenv
# Security floor: pypdf parses UNTRUSTED IR/filing PDFs, so it sits on the
# attacker-reachable path and carries a heavy CVE stream — keep it current
# (>=6.13.3 clears the 30+ CVEs flagged by the 2026-06-18 SCA).
pypdf>=6.13.3
reportlab
mcp
# LLM access — primary path: Claude Code CLI (`claude -p`) via subprocess. No Python
# SDK dependency. Install separately: see https://code.claude.com/docs/en/setup. The
# CLI honors either ANTHROPIC_API_KEY (metered API billing) or `claude auth login`
# (Pro/Max subscription) — see .env.example.
#
# LLM access — fallback path: Gemini via google-generativeai SDK. Used only when the
# Claude CLI call fails (timeout, non-zero exit, empty output, binary missing). Reads
# GEMINI_API_KEY (or GOOGLE_API_KEY) from .env via python-dotenv. See .env.example.
google-generativeai
# Anthropic Python SDK — used ONLY by execution/submit_saydo_batch.py for the
# message-batches API (which the Claude CLI doesn't expose). Reads
# ANTHROPIC_API_KEY from .env (metered API billing — already the project default;
# see feedback_anthropic_api_key.md). The synchronous LLM path still goes through
# the CLI; this is the off-hours bulk-submission lane only.
anthropic
openpyxl
pydantic
yt-dlp
faster-whisper
beautifulsoup4
markdown
# Free fallback source for live price + earnings calendar (src/sources/) and
# EPS surprise (src/surprise_sources.py). Wraps Yahoo Finance's unofficial
# endpoints — fragile but durable across an FMP downgrade.
yfinance
