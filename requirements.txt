flask
pandas
lxml
requests
python-dotenv
pypdf
reportlab
mcp
# LLM access — primary path: Claude Code CLI (`claude -p`) via subprocess. No Python
# SDK dependency. Install separately: see https://code.claude.com/docs/en/setup, then
# `claude auth login`. ANTHROPIC_API_KEY MUST be unset in the runtime env (silently
# overrides to API billing). src/llm_client.py enforces this with a fail-loud lazy check.
#
# LLM access — fallback path: Gemini via google-generativeai SDK. Used only when the
# Claude CLI call fails (timeout, non-zero exit, empty output, binary missing). Reads
# GEMINI_API_KEY (or GOOGLE_API_KEY) from .env via python-dotenv. See .env.example.
google-generativeai
openpyxl
pydantic
yt-dlp
faster-whisper
pydantic
beautifulsoup4
