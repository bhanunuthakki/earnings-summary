# Approved-IR fixture boundary

These deterministic JSON fixtures are sanitized outputs of the rendered-DOM capture boundary,
not raw live pages. They preserve the fields the pure adapters require: source observation key,
authority URL, raw/rendered-byte SHA-256, selected/visible state, reporting period, links, and
evidence locators. Live raw-byte capture plus a dry run remains a separate activation gate.
