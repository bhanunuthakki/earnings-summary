# Platform backlog

Cross-workspace bugs and features captured via the comment processor (intent=`platform_change`). Entries are tagged by feature area and the originating workspace. Group by area when planning engineering work — every entry should land as ONE renderer/pipeline change applied across all workspaces, not as a per-ticker brief edit.

- [x] **[pipeline]** (free_text · `STATUS` · tab=thesis) — reported 2026-05-22 from AMZN: Fetch these from IR docs or transcripts or FMP data, shouldn't be empty/unknown. **Partially resolved 2026-05-24**: root cause was the comment processor's auto-rebuild running `extract_kpis_from_summaries` WITHOUT `--refresh`, so newly-added tier-1 KPIs whose first LLM extraction pass returned nothing for a given quarter stayed empty forever (per-period idempotency kept the missing ones missing). Fix: auto-rebuild now passes `--refresh` when `edit_structured` applied this round, so new KPI names get a clean re-attempt across all quarters. **Result for AMZN**: 4 of 8 new tier-1 KPIs (AWS RPO, AWS op margin, AWS revenue growth, NA retail op margin) flipped from UNKNOWN to GREEN with extracted values. **Residual**: 4 KPIs still UNKNOWN — sequential $ deltas (AWS net new $) and derived metrics (NA retail ex-ads, capex intensity vs AWS growth, Advertising YoY) need either richer source coverage (IR slides — AMZN has none ingested) or a more sophisticated extractor that can compute derived series. Tracked as a follow-up; analyst can `/extract_kpi` to supply these manually in the meantime. **Closed 2026-06-12 (S17)**: re-ran `extract_kpis_from_summaries --ticker AMZN --refresh` over all 6 quarters; 4 facts inserted for defs 652-654 (AWS revenue growth, op margin, RPO). All 5 residual defs (655-659) confirmed `primary_source='ir_doc'` — the transcript extractor skips them by design and AMZN has no IR docs ingested. Disposition: defs 655 (AWS net new $ QoQ), 656 (NA retail ex-ads), 659 (capex intensity vs AWS growth) KILLED — derived/sequential, not directly disclosed in any text source. Defs 657 (NA retail op margin) and 658 (Advertising YoY) ARE explicitly disclosed by AMZN per quarter but summaries didn't surface them; will populate when AMZN IR docs are ingested or analyst uses `/extract_kpi` to supply values manually.
- [x] **[pipeline]** (free_text · `QUARTER` · tab=earnings) — reported 2026-05-22 from AMZN: Pull a couple of additional quarters of earnings transcripts as well. **Resolved 2026-06-12 (S17)**: ran `process_ir_documents --regenerate-missing --ticker AMZN` which generated Q4 2024 and Q1 2025 summaries from previously-ingested transcripts that lacked cached summaries; Q2 2025 summary also generated. AMZN now has 6 continuous quarters of `.tmp/` summaries (Q4 2024 → Q1 2026).
- [x] **[renderer]** (free_text · `Online Stores 📖` · tab=financials) — reported 2026-05-22 from AMZN: Why id the definition not shown either on hover or click through, investigate bug across reports and fix. **Resolved 2026-05-22: `data/segment_definitions/<T>.json` cache was never populated because `extract_segment_definitions.py` had no wiring into the build pipeline; renderer correctly drops the 📖 mark when no definition is loaded. Fix: wired `_ensure_segment_definitions` into `build_artifacts._build_one` (sha256-keyed cache, ~20s Haiku call only when the 10-K source changes), backfilled cache for all 80 tracked + watchlist + evaluation tickers. AMZN now renders 6 tooltips on the segments tab.**
- [x] **[general]** (free_text · `Execution vs. Exogenous)` · tab=saydo) — reported 2026-05-22 from AMZN: why is this empty? **Resolved (verified 2026-06-12, S17 triage)**: two layered fixes landed since the report — the Say-Do prompt now REQUIRES a `### 4. Adversarial Loop — Attribution (Execution vs. Exogenous)` section (prompt-quality rewrite), and the §6 parser extracts each card's `attribution` from that block (#115 block-based parsing, with a fallback to legacy `Attribution:` label lines). Verified against the live 2026-06-11 AMZN report: the Attribution panel renders the full resolution paragraph for every tracked pair.
- [x] **[renderer]** (free_text · `Q3 2025 → Q4 2025` · tab=saydo) — reported 2026-05-22 from AMZN: Why can't I click into this older say do? I should be able to toggle to a prior report say do if I like. make sure this is not a bug across workspaces. **Resolved (verified 2026-06-12, S17 triage)**: superseded by #272 — the saydo tab renders one full detail block per tracked pair, swapped by the same quarter-selector the earnings tab uses, so every prior pair is clickable inside the current report (the §6 builder scans ALL `.tmp/SayDo_<T>_*.txt` files, newest first). Cross-workspace by construction (shared renderer).
- [ ] **[pipeline]** (peer_comp · `peer_comp` · tab=company) — reported 2026-06-13 from owner (S5 follow-up): the peer-comp panel's *selection* is still the FMP sector/market-cap screen (`load_peer_comp` → `{T}_peers.json`), the root cause of the "shit peers" complaint. S5 added steering (`curate_peers`) + a quality override but NOT a better generator. **Spec'd, not built — see `directives/peer_selection_llm.md`**: add an LLM `peer_selection` call (business-model comparables, structured output, eval-gated, cached) that seeds `load_peer_comp` with FMP corroborating + supplying metrics; one dedicated session.
- [x] **[renderer]** (free_text · `REVENUE ($M)` · tab=company) — reported 2026-05-22 from AMZN: Where available can we add a column for operating profit contribution by segment as well? There should also be a change across workspaces. **Resolved 2026-05-24: extended `SegmentWeighting` with `operating_income_usd_m` + `oi_share_pct` (joined from `segment_facts.operating_income` for the latest quarter), threaded through `_build_weighting_rows` on BOTH the product and geographic breakdown tables, added the columns to all three renderers (workspace, html, markdown). Naming mismatch handled via `_normalize_oi_key` (strips " Segment" suffix + small alias table for AWS/Amazon Web Services). Columns suppress when fewer than 2 rows have OI (avoids lone-row + many-"—" tables). AMZN now shows AWS at 59% of OI on 21% of revenue on the geographic table.**

## LLM evals & pareto-optimizer roadmap (2026-07-01 audit)

Full audit of the eval/routing infrastructure against the llm-ops bar (session gracious-ishizaka). Shipped that day: #722 (SayDo extractor governed + scan markers; was ~$150/mo purpose=NULL), #723 (CANDIDATE_ERRORED verdict separation), straggler purpose registrations. Prod migrated to 0129. **2026-07-02: the meta-eval governance build landed (#749–#754, `directives/meta_eval_governance.md` §10)** — workload inventory, stratified sampler + Wilson switch gate + INSUFFICIENT_FRAME, Opus nominator + monthly frontier research (`candidate_models`), per-query judge criteria, prompt A/B + auto-apply (`prompt_pin_overrides`), the I1–I7 isolation suite, steering-freshness alarm, and `revert_model_switch.py` (+ manual locks). Items below updated accordingly. Open items, ordered by leverage:

- [x] **[llm-ops]** Diagnose + fix the Gemini backend failures in the eval sweep. **Root cause found 2026-07-01: not our bug** — Google discontinued individual-account "Login with Google" OAuth to gemini-cli on 2026-06-18 (`IneligibleTierError`, 100% of calls, confirmed live across 3 models; unrelated to version/model-id/timeout). Google's announced replacement (Antigravity CLI `agy` / the `google-antigravity` SDK) was installed and evaluated live, then rejected: both are agentic coding-assistant harnesses with no model pinning and no structured/usage output, not a completion API. **Fix**: `src/llm/gemini_backend.py` rewritten to call the Gemini Developer API directly (metered `GEMINI_API_KEY`, same key + SDK `src/llm/fallback.py` already used) — full CLI/subprocess/OAuth machinery removed, self-anneal now queries the live model catalog instead of scraping gemini-cli's (now-retiring) docs. Grounded cost estimate at current volume: ~$2/mo. Details + rejected-alternatives writeup: `directives/gemini_backend.md`. **Residual, NOT fixed by this PR**: the `GEMINI_API_KEY` in `.env` returns `InvalidArgument: API key not valid` live — this predates the migration (it's why the `fallback.py` emergency path has sat behind `LLM_FALLBACK_DISABLED=1` since 2026-06) and is a separate operator action (rotate the key at https://aistudio.google.com/app/apikey). Until that happens, Gemini candidates still fail — now via a correctly-classified `LLMSetupError` naming the fix, not a mystery `CalledProcessError`. Re-run `execution/run_model_eval_sweep.py --sweep-limit 2` after rotating the key to confirm live candidates complete.
- [x] **[llm-ops]** Model-eval loop PR4 (planned, never built): dashboard panel over `model_eval_verdicts` + `model_pin_overrides` + `llm_calls` — per-purpose $/mo, verdict history, realized-savings rollup, CANDIDATE_ERRORED infra flags. The optimizer currently has no surface; verdicts are only queryable by hand. **Built 2026-07-01**: `src/pipeline/model_eval_panel.py` (the **Optimizer** panel, composed into the System → Provenance console beside Evals; `/api/panel/model_eval` route + `#model_eval` shell alias). Four views — anonymous-purpose alarm, active overrides + realized-savings rollup ("≈$X/mo saved by N downgrades" via `model_ladder.estimated_call_usd`), per-(purpose, candidate) verdict history (CANDIDATE_ERRORED as an outline-red infra flag, NOT a quality pill), per-purpose 30d cost split prod-vs-eval-scope. Folds in the anonymous-purpose alarm item below. Tests: `tests/test_model_eval_panel.py`.
- [ ] **[llm-ops]** Verify the weekly eval rungs actually run after the #711 PYTHONUTF8 fix (next window: Sun 10:30). `eval_runs` has 3 rows EVER (last 2026-06-12) — the mode-B rubric harness (8 AUDIT_SPECS, 8 rubrics) has never completed a persisted run; grade_calibration crashed on cp1252 on 06-21 and 06-28. Add an eval-freshness alarm (no eval_runs row in N days → alert) so a dead cron can't go unnoticed for weeks again.
- [ ] **[llm-ops]** news_structuring: $81/mo on Opus (2nd-biggest purpose line) with a 2-case golden set. Thicken the golden set (10+ cases with hard negatives), then run the downgrade sweep on it — Opus→Sonnet at parity would save ~$40-50/mo.
- [ ] **[llm-ops]** Golden-set thickening below n=6: transcript_metadata (4), intake_classifier (4), news_structuring (2), podcast_takeaway_summary (3), provenance_caution (4), injection_canaries (5). ~~Raise decide_switch min_n to >=8-10 for RISKY-tier purposes~~ **done 2026-07-02 (#750): per-tier sampling — RISKY n=16/min_n=16, default 12/8 — plus the pooled Wilson-LB ≥0.70 switch gate.**
- [ ] **[llm-ops]** Judge spot-check has never been run (`execution/spot_check_eval_judge.py`; zero `manual:judge_spot_check` rows). Run once over the next real audit batch; record agreement; escalate per-purpose judge via AuditSpec.judge_model where <0.8.
- [x] **[llm-ops]** Model-ladder restamp cadence: ladder lacks claude-fable-5 and post-3.1 Gemini IDs; exec_comp pins carried stale opus-4-7 until 2026-07. Wire /refresh-frontier (or a quarterly manual pass) to restamp `model_ladder.py` prices/IDs and add new-generation candidates to the sweep. **Superseded 2026-07-02 (#751): `model_frontier_research` (Opus+web, monthly, weekly-orchestrator step 0) re-verifies cross-provider prices and upserts newly-promising models into `candidate_models` — discovered models auto-enter the TEST pool and a frontier change triggers re-nomination. The static ladder remains the price authority for its own rows; hand-restamp it opportunistically.**
- [x] **[llm-ops]** Anonymous-purpose alarm: purpose=NULL traffic reached 21% of monthly cost without any surface noticing (the SayDo bypass ran ~5 weeks). Add a call-health check to the Evals panel rollup: any purpose=NULL or unregistered-purpose rows with cost > $1/30d → warning chip. **Built 2026-07-01** as the lead section of the new Optimizer panel (`model_eval_panel.load_anon_costs` / `_alarm_section`): flags purpose=NULL + unregistered-purpose 30d lines over $1 as red chips; the `lens:*` family + the `LLM_MODELS ∪ registered_purposes() ∪ evals.coverage.META_PURPOSES` universe are treated as known (no false alarms). See the PR4 row above.
- [ ] **[llm-ops]** Uncovered high-value purposes (33 registered purposes have no eval mode): recent_developments ($78/mo, needs a web-tolerant rubric), exec_comp_alignment ($28/mo), saydo_commitment_extract (new — mode-A golden set from hand-labeled transcripts), lens:* family ($25/mo combined, one shared rubric), research_* trifecta, theme_synthesis.
- [ ] **[llm-ops]** `artifact_brief` (auto-brief on capture, #788, Ledger Phase 3) has no eval yet — another uncovered purpose per the row above, called out separately since it's newer and fires on every capture (not yet cost-profiled). Tracked as uncovered, not built.
- [ ] **[llm-ops]** decide_switch direction-aware agreement (carried from the 2026-06-11 finding): when judges split "tie vs candidate-wins" (both pro-downgrade), don't count it as disagreement — company_description-style HOLDs under-switch.

## Monthly Red Team program (2026-07-10 adversarial portfolio review)

Program spec: `directives/monthly_red_team.md`. Findings that motivated it: fake `BEAR_SEED` bears (UBER/WIX/NVO/BKNG bear FV above live), MELI/NU (25% of book) with no scenario block at all, NU compound tripwire prose-only with a lit leg invisible, MELI NIMAL glide (−490bps/yr toward the 15% floor) reading plain OK, corrupt NU customer KPI series, book vol ~22-27% vs wealthplan CMA 16%.

- [x] **[risk]** PR1: coverage gate + bear-realism lint + per-name bear deltas (`BEAR_SEED` → labeled fallback with provenance). **Shipped #854 (2026-07-11).**
- [x] **[evaluator]** PR2: trajectory WARN soft rules + encode NU net-adds/penetration compound + MELI NIMAL glide + KPI monotonicity guard + NU series data fix. **Shipped #855; NU fact 42198 unit-fixed in prod, fact 42175 dip = open note 58.**
- [x] **[pipeline]** PR3: naked-position gate (nightly stage 0h; downside trigger + realistic bear + thesis ≤90d; violations = standing chips). **Shipped #858 + VTI/VOO price-history rider; live gate went 7→1 violations after the data pass.**
- [x] **[risk]** PR4: fat-tail multivariate-t book MC + joint-LatAm event-corr stress + FLKR price-history cache + quarterly wealthplan CMA export. **Shipped #853.**
- [x] **[llm-ops]** PR5: First-Saturday red-team pipeline (rotating lenses, cross-book passes, `red_team_items` 0147, quota-window registration, per-item degrade). **Shipped #857; task registered, next run 2026-08-01 10:00.** Eval coverage for `red_team_attack`/`red_team_cross_book` still open under "uncovered purposes".
- [x] **[ui]** PR6: forced response loop — REFUTE (ledger) / ACCEPT (auto-artifact) / DEFER-once (then Home-band escalation), month-close gate, Telegram `/redteam`. **Shipped #861.** Note: capture_poller task is currently Disabled → Telegram commands inert until re-enabled.
- [x] **[calibration]** PR7: scored-miss gate on re-underwrites (Brier entry before new thesis admitted) + Decision P&L wiring + January annual-letter draft (manual, no cron). **Shipped #860.** `annual_letter` purpose also uncovered by evals.
- [x] **[data]** honest thesis bears persisted for all six flagged names (NU −69% $4.08, MELI −60% $724, UBER/WIX −39%, BKNG −32%, NVO −31%; notes say "pending owner review") + prod migration 0147 + FLKR thesis-mirror sync. **Done 2026-07-11 (PR8 #859 + data pass; backup `portfolio.db.bak_redteam_datapass_20260711`).**
- [ ] **[risk]** FLKR residual naked-position violation: an ETF can't have a DCF bear — owner decision needed (ETF exemption in position_guard vs a non-DCF scenario source for the memory-cycle bear)
- [ ] **[risk]** Tail-stress headline stuck at ~69% modeled by construction (index/cash sleeves have no DCF bears) — needs an ETF-sleeve stress treatment so the coverage gate can clear 90%
