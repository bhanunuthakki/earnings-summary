# Platform backlog

Cross-workspace bugs and features captured via the comment processor (intent=`platform_change`). Entries are tagged by feature area and the originating workspace. Group by area when planning engineering work — every entry should land as ONE renderer/pipeline change applied across all workspaces, not as a per-ticker brief edit.

- [x] **[pipeline]** (free_text · `STATUS` · tab=thesis) — reported 2026-05-22 from AMZN: Fetch these from IR docs or transcripts or FMP data, shouldn't be empty/unknown. **Partially resolved 2026-05-24**: root cause was the comment processor's auto-rebuild running `extract_kpis_from_summaries` WITHOUT `--refresh`, so newly-added tier-1 KPIs whose first LLM extraction pass returned nothing for a given quarter stayed empty forever (per-period idempotency kept the missing ones missing). Fix: auto-rebuild now passes `--refresh` when `edit_structured` applied this round, so new KPI names get a clean re-attempt across all quarters. **Result for AMZN**: 4 of 8 new tier-1 KPIs (AWS RPO, AWS op margin, AWS revenue growth, NA retail op margin) flipped from UNKNOWN to GREEN with extracted values. **Residual**: 4 KPIs still UNKNOWN — sequential $ deltas (AWS net new $) and derived metrics (NA retail ex-ads, capex intensity vs AWS growth, Advertising YoY) need either richer source coverage (IR slides — AMZN has none ingested) or a more sophisticated extractor that can compute derived series. Tracked as a follow-up; analyst can `/extract_kpi` to supply these manually in the meantime. **Closed 2026-06-12 (S17)**: re-ran `extract_kpis_from_summaries --ticker AMZN --refresh` over all 6 quarters; 4 facts inserted for defs 652-654 (AWS revenue growth, op margin, RPO). All 5 residual defs (655-659) confirmed `primary_source='ir_doc'` — the transcript extractor skips them by design and AMZN has no IR docs ingested. Disposition: defs 655 (AWS net new $ QoQ), 656 (NA retail ex-ads), 659 (capex intensity vs AWS growth) KILLED — derived/sequential, not directly disclosed in any text source. Defs 657 (NA retail op margin) and 658 (Advertising YoY) ARE explicitly disclosed by AMZN per quarter but summaries didn't surface them; will populate when AMZN IR docs are ingested or analyst uses `/extract_kpi` to supply values manually.
- [x] **[pipeline]** (free_text · `QUARTER` · tab=earnings) — reported 2026-05-22 from AMZN: Pull a couple of additional quarters of earnings transcripts as well. **Resolved 2026-06-12 (S17)**: ran `process_ir_documents --regenerate-missing --ticker AMZN` which generated Q4 2024 and Q1 2025 summaries from previously-ingested transcripts that lacked cached summaries; Q2 2025 summary also generated. AMZN now has 6 continuous quarters of `.tmp/` summaries (Q4 2024 → Q1 2026).
- [x] **[renderer]** (free_text · `Online Stores 📖` · tab=financials) — reported 2026-05-22 from AMZN: Why id the definition not shown either on hover or click through, investigate bug across reports and fix. **Resolved 2026-05-22: `data/segment_definitions/<T>.json` cache was never populated because `extract_segment_definitions.py` had no wiring into the build pipeline; renderer correctly drops the 📖 mark when no definition is loaded. Fix: wired `_ensure_segment_definitions` into `build_artifacts._build_one` (sha256-keyed cache, ~20s Haiku call only when the 10-K source changes), backfilled cache for all 80 tracked + watchlist + evaluation tickers. AMZN now renders 6 tooltips on the segments tab.**
- [x] **[general]** (free_text · `Execution vs. Exogenous)` · tab=saydo) — reported 2026-05-22 from AMZN: why is this empty? **Resolved (verified 2026-06-12, S17 triage)**: two layered fixes landed since the report — the Say-Do prompt now REQUIRES a `### 4. Adversarial Loop — Attribution (Execution vs. Exogenous)` section (prompt-quality rewrite), and the §6 parser extracts each card's `attribution` from that block (#115 block-based parsing, with a fallback to legacy `Attribution:` label lines). Verified against the live 2026-06-11 AMZN report: the Attribution panel renders the full resolution paragraph for every tracked pair.
- [x] **[renderer]** (free_text · `Q3 2025 → Q4 2025` · tab=saydo) — reported 2026-05-22 from AMZN: Why can't I click into this older say do? I should be able to toggle to a prior report say do if I like. make sure this is not a bug across workspaces. **Resolved (verified 2026-06-12, S17 triage)**: superseded by #272 — the saydo tab renders one full detail block per tracked pair, swapped by the same quarter-selector the earnings tab uses, so every prior pair is clickable inside the current report (the §6 builder scans ALL `.tmp/SayDo_<T>_*.txt` files, newest first). Cross-workspace by construction (shared renderer).
- [ ] **[pipeline]** (peer_comp · `peer_comp` · tab=company) — reported 2026-06-13 from owner (S5 follow-up): the peer-comp panel's *selection* is still the FMP sector/market-cap screen (`load_peer_comp` → `{T}_peers.json`), the root cause of the "shit peers" complaint. S5 added steering (`curate_peers`) + a quality override but NOT a better generator. **Spec'd, not built — see `directives/peer_selection_llm.md`**: add an LLM `peer_selection` call (business-model comparables, structured output, eval-gated, cached) that seeds `load_peer_comp` with FMP corroborating + supplying metrics; one dedicated session.
- [x] **[renderer]** (free_text · `REVENUE ($M)` · tab=company) — reported 2026-05-22 from AMZN: Where available can we add a column for operating profit contribution by segment as well? There should also be a change across workspaces. **Resolved 2026-05-24: extended `SegmentWeighting` with `operating_income_usd_m` + `oi_share_pct` (joined from `segment_facts.operating_income` for the latest quarter), threaded through `_build_weighting_rows` on BOTH the product and geographic breakdown tables, added the columns to all three renderers (workspace, html, markdown). Naming mismatch handled via `_normalize_oi_key` (strips " Segment" suffix + small alias table for AWS/Amazon Web Services). Columns suppress when fewer than 2 rows have OI (avoids lone-row + many-"—" tables). AMZN now shows AWS at 59% of OI on 21% of revenue on the geographic table.**
