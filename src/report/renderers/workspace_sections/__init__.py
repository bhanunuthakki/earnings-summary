"""Per-section renderer modules for the tabbed workspace document.

``workspace_html.render`` is the only entry point; it composes these
modules. Import section renderers from their module here, not from
``workspace_html`` (whose flat names remain only as back-compat
re-exports)."""
